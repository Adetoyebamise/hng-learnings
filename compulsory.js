const name = "adetoyebamise"
const id = "unknown"
const email = "adetoyebamise@gmail.com"
const output = (name, id, email) => {
  return `Hey there, this is ${name} with ID ${id} and email ${email}`
}
console.log(output(name, id, email))
